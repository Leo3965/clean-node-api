describe('AccountRepository', () => {
  beforeAll(async () => {
    // await MongoHelper.connect()
  })

  beforeEach(async () => {
    // const accCol = MongoHelper.getCollection('accounts')
    // await accCol.deleteMany({})
  })

  afterAll(async () => {
    // await MongoHelper.disconnect()
  })

  test('Should return an account on successfully', async () => {
    // const sut = new AccountMongoRepository()
    // const acc = await sut.add({
    //   name: 'John Doe',
    //   email: 'john@example.com',
    //   password: '123456'
    // })

    // expect(acc).toBeTruthy()
    // expect(acc.id).toBeTruthy()
    // expect(acc.name).toBe('John Doe')
    // expect(acc.email).toBe('john@example.com')
    // expect(acc.password).toBe('123456')
  })
})
